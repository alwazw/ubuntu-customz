# --------------
# Ports Assigned
# --------------
# PAUSED: not wired into 05_stitch.sh (never was, across any version).
# Unified port mapping across VMs is a premature idea for now — a single
# fixed-port file causes collisions when synced/reused across machines.
# Keep this parked here until that design is actually ready; do not move
# it back into var/ (which 05_stitch.sh syncs) until then.

OPENWEBUI_PORT=3000
LITELLM_PORT=4000
N8N_PORT=5678
QDRANT_PORT=6333
OLLAMA_PORT=11434
SEARXNG_PORT=8080
OPENCLAW_PORT=18789
AGENTZERO_PORT=50080
OMNIROUTE_PORT=20128
